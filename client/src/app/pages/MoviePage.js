import { BaseLayout } from '../layouts';
import { MovieList } from '../components/project';

const MoviePage = () => {

  return (
    <BaseLayout>
      <MovieList/>
    </BaseLayout>
  );
};

export default MoviePage;