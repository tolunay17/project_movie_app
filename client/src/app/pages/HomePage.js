import { BaseLayout } from '../layouts';
import { HomeList } from '../components/project';

const HomePage = () => {
  
  return (
    <BaseLayout>
      <HomeList/>
    </BaseLayout>
  );
};

export default HomePage;